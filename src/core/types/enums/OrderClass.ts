/** @format */

// The following table specifies all supported bracket order components.
export enum OrderClass {
  //  Primary order
  Primary = 'Primary',
  //  Profit exit order
  Limit = 'Limit',
  //  Loss exit order
  StopLoss = 'StopLoss',
}
